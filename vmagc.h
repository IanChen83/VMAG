#include <vector>

using namespace vmag;

namespace vmagc{
    class vmagc{
        public:
            std::vector<vmag> carriers;
            void VMAGC();
            void reset();
            void dispatch(vmag&, vmag&);
        private:.

    }
}
